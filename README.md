# ragent-st

这是 Ragent 的渐进式教学重建项目，不是原项目的精简副本。

第 1～3 课已经完成。第 4 课正在增加固定向量、余弦相似度和 Top-K 排序，用确定性测试理解语义检索核心；真实 Embedding 服务尚未接入。项目仍只使用 Java 17、Maven 和 JUnit 5。

课程资料位于相邻目录 `../tutorial/`，长期约束见 `../tutorial/工作守则.md`。

## 当前验证命令

```bash
mvn test
```

当前应有九个测试全部通过：六个已有问答/文件测试，以及三个向量检索测试。
