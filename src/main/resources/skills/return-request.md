# 创建退货申请

适用场景：用户明确要求为已经存在的订单创建退货申请。

执行顺序：

1. 先调用 `order_status`，确认订单存在并取得当前状态。
2. 只有状态为 `delivered` 时才能继续；其他状态先向用户解释，不能创建申请。
3. `reason` 必须来自用户原话，不能替用户编造。
4. 调用 `return_request_create` 前必须展示订单号和原因，等待用户明确确认。
5. 确认后只能执行已经展示的参数；不得在确认阶段更换订单号或原因。
6. 成功后把远端返回的退货申请编号告诉用户。
