# 第 15 课：只靠一种检索，精确编号和自然语言难以兼得

## 先看两条通路

本课没有改变上传通路，上传仍然是：

```text
上传 HTTP
  → 摄取任务
  → 解析、分块、Embedding
  → knowledge_chunk 表保存正文和向量
```

本课改变的是回答通路：

```text
问题 HTTP
  → ConversationQuestionService 保存用户消息
  → IntentRoutingService 改写、拆问并确定知识库作用域
  → HybridRetrievalService 并行执行
       ├─ VectorSearchChannel：问题 → Embedding → pgvector 语义候选
       └─ KeywordSearchChannel：问题 → 编号/短语 → 文本精确候选
  → 按 Chunk id 去重并用 RRF 融合名次
  → BailianEvidenceReranker 精排候选
  → BailianClient 把前 2 条证据和问题交给 Chat 模型
  → QuestionResponse 返回答案、来源和 evidence
```

一句话概括：上传时文档已经被分块、向量化并存进 PostgreSQL；回答时同一个问题同时走“语义找相似”和“文字找精确”，再把合并后的少量证据交给大模型。

## 先定位本课在整条链的哪一层

一条完整问答可能有很多层，不能看到一个 `List` 就把它当成最终答案：

```text
父问题
  → 会话层：近期消息 + 历史摘要
  → 拆问层：一个父问题变成多个子问题
  → 规划/路由层：每个子问题决定允许访问哪些知识库
  → 召回层：一个子问题可以同时走 vector 和 keyword
  → 证据层：每个 Chunk 记录通道、原始分、RRF 分、精排分
  → 子问题答案层：把证据交给 Chat，得到一个 KnowledgeAnswer
  → 父问题聚合层：RoutedKnowledgeAnswer 合并多个子问题答案
  → HTTP 包装层：QuestionResponse 加上会话和观察字段后返回
```

本课主角是最深处的“召回层和证据层”：`HybridRetrievalService` 并不知道父问题，也不负责会话摘要；它只接受一个已经可以独立检索的子问题和一个已经确定的知识库作用域。这样才能解释为什么第 14 课的路由结果会原样传到第 15 课，而不是在这里重新选择知识库。

### 这些数据类怎样沿流程变化

| 数据类 | 所处层 | 它保存什么 | 它不代表什么 |
|---|---|---|---|
| `SearchChannelResult` | 单通道召回层 | 一条通道的有序候选列表和耗时 | 不是最终证据，也不是答案 |
| `RetrievedEvidence` | 单 Chunk 证据层 | 一个 Chunk 的标题、正文、通道、向量/关键词/RRF/精排分 | 不是大模型生成的回答 |
| `KnowledgeAnswer` | 单个子问题答案层 | 一个子问题的回答、主要来源和最终证据 | 不包含多个子问题的整体规划 |
| `RoutedKnowledgeAnswer` | 父问题聚合层 | 多个子问题的回答、来源、计划和证据 | 还不是 HTTP 最终字段包装 |
| `QuestionResponse` | HTTP 响应层 | 父问题答案、会话 ID、改写问题、计划和证据 | 不负责任何检索算法 |

你可以把它们看成从“数据库行”到“业务层快照”的不同高度：字段看起来有重复，是因为上层需要携带下层结果，但每一层都增加了自己的边界信息。

## 为什么上一课的单路向量检索不够

第 14 课以后，问题已经可以被正确地改写、拆分并限制到某个知识库，但具体检索仍然只有向量通道。向量很适合“设备无法启动”和“按电源键没有反应”这种意思相近但字面不同的说法，却可能把 `WX-2048` 这样的错误码看成普通字符。反过来，纯关键词检索能保护编号，却不理解同义表达。

本课只解决这个真实问题：两种召回都保留，不能直接把两种分数相加，因为余弦相似度和关键词出现次数不是同一把尺子。先分别取名次，再用 Reciprocal Rank Fusion（RRF，倒数排名融合）统一比较；之后只把很小的候选集交给精排模型，最后最多把两条证据放进回答 Prompt。

## 完整执行链和对象职责

1. `IntentRoutingService` 把第 14 课的 `knowledgeBaseIds` 原样交给 `BailianRagAssistant`，所以混合检索不会越过已选定的知识库。
2. `HybridRetrievalService` 为每个 `SearchChannel` 创建一个 `CompletableFuture`。它使用 Spring 的 `retrievalExecutor` 线程池并行等待两路结果；某一路报错只变成空结果，不影响另一条路。
3. `VectorSearchChannel` 先调用 `BailianClient.createEmbedding`，然后让 `KnowledgeRepository.searchVectorCandidates` 用 pgvector 按余弦距离取前 8 条。
4. `KeywordSearchChannel` 从问题中取出字母数字编号、数字和 2～8 个汉字短语，再让仓库读取同一作用域的成功片段并计算精确命中分。
5. `HybridRetrievalService.fuseByRrf` 以 Chunk 的 `id` 去重。一个 Chunk 如果被两条通道都找到，就保留 `channels=["keyword","vector"]` 以及两种原始分数；它在两个通道都靠前时，RRF 分更高。
6. 候选先截到 6 条，再交给 `BailianEvidenceReranker`。当前为了复用已经配置好的百炼 Chat 网关，模型被要求返回 `{"results":[{"index":0,"score":0.98}]}`；这一步是“精排契约”的教学实现，不是把官方专用 Rerank 地址硬编码进主链。
7. `BailianRagAssistant` 只把精排后的前 2 条转成 `KnowledgeEntry`，交给 `BailianClient.generateAnswer`。`KnowledgeAnswer` 同时保留结构化 `evidence`，所以测试和 HTTP 调用者能看到模型实际使用了哪些证据。

```text
问题 q
  ├─ vector：Embedding(q) → [chunk A, chunk B, ...]
  └─ keyword：提取 WX-2048 → [chunk A, ...]
                 ↓
          id 去重 + RRF 名次融合
                 ↓  候选最多 6 条
             Chat 精排
                 ↓  上下文最多 2 条
          Chat 生成受证据约束的回答
```

## 测试里的真实数据如何流动

`HybridRetrievalApiLiveIT` 先上传：

```markdown
# 设备售后
## 故障代码 WX-2048
遇到 WX-2048 时请先断电 30 秒，再重新启动。
## 无法开机
设备按下电源键没有反应时，请检查电源线。
```

第一问是 `设备报错 WX-2048 怎么处理？`。关键词通道命中 `WX-2048`，向量通道也找到同一 Chunk；返回的中间 evidence 类似：

```json
{
  "title": "设备售后 / 故障代码 WX-2048",
  "channels": ["keyword", "vector"],
  "vectorScore": 0.88,
  "keywordScore": 28.0,
  "rrfScore": 0.0328,
  "rerankScore": 0.98
}
```

第二问是 `设备按电源键没有反应怎么办？`。它没有固定错误码，向量通道把它带到 `无法开机`，关键词通道可能没有命中；这不是失败，而是“这一问主要由语义通道负责”。测试故意打印两次响应，帮助观察通道列表、原始分数、RRF 分数、精排分数和最终答案。

## 为什么需要这些新类

- `SearchChannel`：两种检索已经有不同输入处理和数据库动作，继续塞进一个方法会让“并行、异常隔离、融合”与具体算法搅在一起。接口只约定“给我问题，返回候选”。
- `SearchQuery` / `SearchChannelResult`：把作用域、问题和候选耗时作为一次通道执行的输入输出，避免在异步闭包里依赖零散变量。
- `RetrievedEvidence`：`KnowledgeEntry` 只适合交给答案模型；本课还要携带 channel、向量分、关键词分、RRF 分和精排分，所以新增一个“检索过程中的证据”对象。
- `HybridRetrievalService`：它是本课主角，负责调度和后处理，不负责写 SQL，也不负责拼 Chat JSON。
- `EvidenceReranker`：精排模型以后可能更换；现在只有百炼实现，所以先抽出真正被两处需要的契约，不预埋更多 Provider。

## 几个容易混淆的技术点

### 为什么不把两个分数直接相加？

向量分大约在 0～1 之间，关键词分可能是 28、40；直接相加会让关键词权重天然更大。RRF 不关心原始分数，只看每条通道的第几名：第 `rank` 名贡献 `1 / (60 + rank)`。同一条证据同时出现在两条通道里，就得到两份贡献。

### `CompletableFuture` 和线程池解决什么？

一个 HTTP 请求线程不必先等向量查询结束，再等关键词查询结束。两个 `supplyAsync` 把等待工作放进受限的 `retrievalExecutor`；请求线程最后 `join` 两个结果。每个请求的候选列表独立，线程池和 Spring 单例服务是共享的，但本课没有把候选保存到共享可变字段中。

### 为什么通道异常要变成空列表？

向量模型短暂超时不应让关键词编号检索也完全失效，反过来也一样。`searchSafely` 是本课明确的降级边界；若两路都空，系统才返回“没有找到足够相关的知识”。

### 精排为什么也允许降级？

精排是候选的第二次排序，不是知识本身。百炼返回格式不对或网络失败时，`BailianEvidenceReranker` 保留 RRF 候选继续回答；如果模型确实返回了分数但最高分低于 `0.25`，才触发证据门槛并拒绝把无关内容送给答案模型。

### 这里的 Rerank 是官方独立模型吗？

百炼确实提供专用 Rerank 能力，但它需要单独的工作空间服务地址和模型配置。本课先复用已经验证过的 Chat 网关，只练习“候选输入 → 排序 JSON → 重新排序”的数据契约。后续若换成专用 Rerank，只替换 `EvidenceReranker` 实现，`HybridRetrievalService` 的输入输出不变。

### `candidates` 这样的 JSON 是协议规定的吗？

不是百炼 Chat Completions 固定规定的字段。`messages`、`role`、`content` 是 Chat 协议的一部分；而本课把候选写成：

```json
[
  {"index":0,"title":"...","content":"..."},
  {"index":1,"title":"...","content":"..."}
]
```

是我们在用户消息正文里约定的一种清晰表示。远端模型收到的最终仍然是字符串，模型可以阅读 JSON 文本；`index` 让模型返回结果时能准确指回原候选。也可以把多个选项拼成普通文本，但 JSON 更容易保留边界、字段和编号，也更方便 Java 用 Jackson 解析。它不是“远端自动理解的魔法格式”，真正起作用的是我们在 system message 中明确告诉模型输入和输出格式，并在 Java 端校验返回值。

### 当前有哪些手工文字规则，以后可能需要模型或专用检索替代？

本课为了让数据流透明，保留了几处刻意简单的规则：

- `IntentPlanner` 用知识库名称路径的字符包含关系拆分和打分；复杂同义词、拼写错误和多语言名称可能判断失误。
- `KeywordSearchChannel` 用正则提取英文编号、数字和连续汉字；它不知道真正的词边界，也不会做分词、同义词扩展或领域词典。
- `KnowledgeRepository.keywordScore` 用字符串出现次数和词长加权；它没有 BM25、倒排索引、停用词和字段权重。
- `KeywordSearchChannel` 先读取作用域内成功片段，再在 Java 中计分；数据量变大后应把搜索下推到数据库全文索引或专门搜索系统。
- `BailianClient.rerank` 用 Chat 模型按约定 JSON 返回分数；生产环境更适合使用专用 Rerank 接口，并处理模型重复编号、漏项和不稳定分数。
- `IntentRoutingService.combineAnswers` 直接编号拼接多个子答案；多个父问题、跨领域冲突或需要统一口径时，应增加专门的合成步骤。

这些不是本课遗漏的 bug，而是当前教学边界：先让每一层的数据和职责看得见，需求真正逼出性能、准确率或一致性问题时，再替换某一层。

## 代码阅读顺序（聊天框会再次给出可点击路径）

1. 先看测试主方法，确认上传文本、两种问题、断言和打印的中间 evidence：`HybridRetrievalApiLiveIT.shouldCombineExactCodeAndSemanticRetrieval`。
2. 看问答入口如何把证据带到 HTTP 响应：`QuestionResponse`、`ConversationQuestionService`、`IntentRoutingService.answer`。
3. 看主编排：`HybridRetrievalService.retrieve`，重点看并行、去重、RRF、候选上限和证据门槛。
4. 分别看 `VectorSearchChannel.search` 和 `KeywordSearchChannel.search`，再看 `KnowledgeRepository.searchVectorCandidates`、`searchKeywordCandidates` 的 SQL 和参数顺序。
5. 看 `RetrievedEvidence` 如何从单通道对象变成融合对象，再看 `BailianEvidenceReranker` 和 `BailianClient.rerank` 的模型输入输出。
6. 最后回到 `BailianRagAssistant.answerFromDatabase` 与 `BailianClient.generateAnswer(List<KnowledgeEntry>)`，确认最终 Prompt 只携带前两条证据。

## 本课边界

本课没有实现 Elasticsearch/BM25、缓存、分布式任务、真正的多租户权限或完整引用展示；关键词通道为了可读性读取作用域内成功片段后在 Java 中计分。真实数据量增大后，这会成为下一步性能需求，而不是现在提前设计的理由。

## 你现在要做什么

打开 `ragent-st` 目录，在 VS Code 左侧测试资源管理器中找到 `HybridRetrievalApiLiveIT`，点击测试方法 `shouldCombineExactCodeAndSemanticRetrieval` 左侧的绿色三角。确认 Docker Desktop 正常运行、永久百炼配置仍在 VS Code WSL Server 中生效。测试会启动临时 pgvector 容器并真实调用 Embedding、精排和 Chat，可能产生少量模型费用。

通过后不要直接生成提交文本。请先告诉姐姐你是否理解了“关键词通道、向量通道、RRF、精排、答案生成”这条链，以及测试输出中的 evidence。你理解并测试完成后，需要你指定本课 commit 文本；姐姐再给出检查、commit 和 push 命令，由你亲手执行。
